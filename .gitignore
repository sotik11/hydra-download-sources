node_modules/
*.log

# secrets
.env
.env.local

# incremental state — big, rebuilt on demand, persisted via Actions cache
data/*.state.json

# local run backups / raw dumps — not source
data/*.prev.json
data/*.bak.json
data/*.json.backup
data/*.backup
